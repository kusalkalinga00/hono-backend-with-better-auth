{
  "name": "hono-backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down"
  },
  "dependencies": {
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
